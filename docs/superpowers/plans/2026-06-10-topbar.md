# 主区顶栏 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 主区顶部常驻选项栏:左 agent/会话面包屑,最右工作区文件按钮;账户菜单的文件入口移除。

**Architecture:** 纯前端表现层。新 `TopBar.tsx`(订阅式读 agents/sessions 缓存 + store),挂进 `App.tsx` 主列顶部;`AccountMenu` 删一项。

**Tech Stack:** React19 + react-query + zustand + lucide-react + vitest。

参考 spec:`docs/superpowers/specs/2026-06-10-topbar-design.md`

---

## Task 1: TopBar 组件(TDD)

**Files:** Create `frontend/src/components/TopBar.tsx`、Test `frontend/src/components/TopBar.test.tsx`

- [ ] **Step 1: 写失败测试**(夹具:store userId/agentId/sessionId + spy listAgents/listSessions;三用例:面包屑双段、无会话单段、点按钮 `fileDrawerOpen` 翻转)
- [ ] **Step 2: 跑失败** `cd frontend && npx vitest run src/components/TopBar.test.tsx`
- [ ] **Step 3: 实现**(spec 的外观/数据流;`header` 元素;按钮 `aria-label="工作区文件"`)
- [ ] **Step 4: 跑通过 + tsc**
- [ ] **Step 5: 提交** `feat(frontend): TopBar with breadcrumb + workspace-files button`

## Task 2: 接入 App + 移除 AccountMenu 旧入口

**Files:** Modify `frontend/src/App.tsx`、`frontend/src/components/AccountMenu.tsx`、`frontend/src/components/AccountMenu.test.tsx`

- [ ] **Step 1: 测试先行**:AccountMenu.test 的「工作区文件」断言改为 `queryByText(...)` 不在(先跑应 FAIL——项还在)
- [ ] **Step 2: 实现**:App `<main>` 内 `<TopBar />` 置于 `<ChatView />` 前;AccountMenu 删工作区文件按钮 + `Folder`/`toggleFileDrawer` 引用 + docstring 更新
- [ ] **Step 3: 跑通过 + tsc**
- [ ] **Step 4: 提交** `feat(frontend): mount TopBar; account menu drops files entry`

## Task 3: 回归 + 审查 + 收尾

- [ ] 前端全量 `npx tsc --noEmit && npx vitest run`(全绿)
- [ ] Fable 5 对抗审查(diff 自取;重点:布局链 min-h-0/shrink-0、backdrop-filter 与浮层无新交互、面包屑空态、缓存订阅)
- [ ] PR → CI 绿 → 等合并指令
